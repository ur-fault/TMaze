name: TMaze
description: Game where you solve mazes in the terminal
authors:
- ur-fault
init:
  global:
    - program: cargo
      args:
      - build
      - --release
run:
  default:
    program: cargo
    args:
    - run
    - --release
checks:
  global:
  - program: cargo
    args:
    - --version
